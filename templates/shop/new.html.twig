{% extends 'base.html.twig' %}

{% block title %}Nouveau commerce{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center align-items-center">
            <div class="d-flex flex-column">
                <h1>Ouvrir un commerce en ligne</h1>
                {{ include('shop/_form.html.twig', {'button_label': 'Créer'}) }}
            </div>
        </div>
    </div>

    <a href="{{ path('shop') }}">back to list</a>
{% endblock %}
